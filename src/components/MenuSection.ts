import { menuItems, MenuCategory } from '../data/menuItems';\nimport { ProductCard } from './ProductCard';\n\nexport function MenuSection(category: MenuCategory): HTMLElement {\n  const section = document.createElement('section');\n  section.className = 'mb-12';\n  const title = document.createElement('h2');\n  title.className = 'text-2xl font-bold mb-6 text-black';\n  title.textContent = category;\n  section.appendChild(title);\n\n  const grid = document.createElement('div');\n  grid.className = 'grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6';\n  menuItems.filter(i => i.category === category).forEach(item => {\n    grid.appendChild(ProductCard(item));\n  });\n  section.appendChild(grid);\n  return section;\n}\n
